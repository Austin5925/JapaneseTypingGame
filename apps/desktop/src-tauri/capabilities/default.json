{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — wires up `core:default` and the app's custom commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "core:window:default"
    }
  ]
}
