# =============================================================================
# 假名打字通 / Kana Typing — .gitignore
#
# 这是一个 PUBLIC 仓库 (Austin5925/JapaneseTypingGame)。
# 任何内部产品判断、技术决策、Claude 上下文都必须在这里被排除，
# 不能因为方便而临时关掉某条规则。
# =============================================================================

# -----------------------------------------------------------------------------
# Private project memory & internal docs (仓库公开 → 这些必须始终 gitignored)
# -----------------------------------------------------------------------------
# 内部开发文档：产品判断、技术架构、计划、复盘、调研。整目录排除。
devdocs/

# 内部ui开发文档
handoff/

# 第三方/外部协作生成的语料中间产物（修订报告、合并 JSON、覆盖率统计）。
# 上线词汇会被复制到 content/official/ 入库，imports/ 是源副本，不入库。
content/imports/
content/official/_archive/

# Claude / AI agent 的项目上下文。包含产品判断、工程纪律、踩坑记录，
# 不希望以原文形式公开。和 CEOAssistant 同模式。
CLAUDE.md

# Codex / OpenAI agent 本地项目记忆。包含私有执行摘要、工程判断、会话上下文。
AGENTS.md
agents.md
CODEX.md
codex.md
.codex/

# Claude Code 本地工程数据（slash command、本地 hooks、临时记忆）
.claude/
.claude-code/

# Cursor / 其他 AI 编辑器本地配置
.cursor/
.aider*

# -----------------------------------------------------------------------------
# Environment / secrets (永不入库；只放 .env.example)
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key

# -----------------------------------------------------------------------------
# Node / pnpm
# -----------------------------------------------------------------------------
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# -----------------------------------------------------------------------------
# Vite / 前端构建产物
# -----------------------------------------------------------------------------
dist/
dist-ssr/
build/
out/
.next/
.turbo/
.vite/
*.local

# -----------------------------------------------------------------------------
# Tauri / Rust
# -----------------------------------------------------------------------------
# Cargo build cache
**/target/
# Tauri runtime bundles + 自动生成产物
**/src-tauri/gen/
**/src-tauri/WixTools/
# Tauri 调试工件
*.app
*.dmg
*.msi
*.exe
*.AppImage
*.deb
*.rpm

# Tauri icon generator also produces iOS / Android variants; we don't ship those
# platforms in v0.x, so keep their assets out of the public repo. macOS .icns,
# Windows .ico, and the web favicon-style PNGs at icons/*.png stay tracked.
apps/desktop/src-tauri/icons/ios/
apps/desktop/src-tauri/icons/android/

# -----------------------------------------------------------------------------
# SQLite local DB / 用户学习数据 (绝不入库)
# -----------------------------------------------------------------------------
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.db
*.db-journal
local-data/
user-data/

# 但允许仓库内的官方迁移 SQL / fixture (放在 migrations/ 或 fixtures/，不要叫 .sqlite)

# -----------------------------------------------------------------------------
# Audio / asset working files (大体积二进制, 走 LFS 或单独发布)
# -----------------------------------------------------------------------------
*.wav
*.aif
*.aiff
*.flac
# 注意：mp3 / ogg 是最终成品，需要时再单独 ! 反向豁免；默认不强制忽略。

# -----------------------------------------------------------------------------
# Test / coverage / playwright artifacts
# -----------------------------------------------------------------------------
coverage/
.nyc_output/
test-results/
playwright-report/
playwright/.cache/
.vitest/

# -----------------------------------------------------------------------------
# IDE / OS
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
!.vscode/extensions.json
!.vscode/settings.shared.json
.idea/
*.swp
*.swo
*.iml

# -----------------------------------------------------------------------------
# Logs / temp
# -----------------------------------------------------------------------------
*.log
logs/
tmp/
.tmp/
.cache/

# Playwright artefacts
tests/e2e/screenshots/
tests/e2e/test-results/
playwright-report/
